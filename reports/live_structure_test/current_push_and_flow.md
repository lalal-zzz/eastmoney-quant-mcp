# 当前结构预警结果与运行流程

生成日期：2026-09-18

示例标的：600000 浦发银行

本地日线截止：2026-09-04
状态：研究快照，数据已滞后，未发送外部通知

## 当前可生成的推送内容

```text
[结构位置提醒][未实时发送]
标的：600000 浦发银行
周期：日线
截止：2026-09-04
收盘：9.43
位置：进入 9.4273–9.4834 共振区

日线结构：
- M 顶颈线 9.00 已跌破，结构评分 0.7109
- W 底颈线 9.28 已突破，结构评分 0.7081
- 另一个 W 底右底已确认，颈线 9.72，尚待突破

高周期背景：
- 周线：848 根，46 个当前监控 W/M，周期已闭合
- 月线：201 根，18 个当前监控 W/M，最新月线可能未闭合

提示：进入位置区不等于反转。等待闭合 K 线、颈线、趋势线或通道事件确认。
```

## 当前数据库状态

- `alert_state`：0 条
- `alert_events`：0 条
- `structure_snapshots`：0 条

因此现在还没有正式订阅、持续轮询或真实推送历史。现有代码能够计算结构快照、生成区域事件、去重并持久化；还缺少把关注标的和关键区注册成规则的订阅层，以及定时读取新行情并调用通知适配器的后台 worker。

## 当前流程

```text
本地日线 / 网络小时线
        ↓
按日、周、月独立生成 OHLCV
        ↓
确认 Pivot（只使用 as-of 当时可见数据）
        ↓
Leg 与多尺度结构
        ↓
趋势线：两点候选、第三点确认、全区间穿越检查
通道：独立平行线或确认主线固定平移
W/M：允许左右持续时间不对称
Fibonacci：按已确认 A→B 波段生成回撤区
        ↓
合并趋势线、通道、颈线、前高低和 Fib 共振区
        ↓
区域状态机
approaching → touched → intrabar crossing → closed-bar breakout/breakdown
        ↓
episode 去重与 SQLite 原子持久化
        ↓
【尚未接入】后台轮询 → 订阅管理 → 微信/邮件/桌面等通知适配器
```

## 已完成

- 日、周、月结构独立识别。
- 趋势线生命周期、破位和水平位转换。
- 平行通道与横向区间。
- 非对称 W/M。
- 方向性 Fibonacci 回撤、投射和共振区。
- approaching、touched、breakout、breakdown、invalidated 事件。
- 事件幂等去重、状态和事件 SQLite 存储。
- 2010 年以后十只股票的结构事件研究。

## 未完成

- 关注列表和预警规则管理接口。
- 分钟/小时行情持续轮询。
- 自动把结构快照转换成活动 ZoneRule。
- worker 崩溃恢复和运行健康状态。
- 外部通知通道及失败重试。
- 十个交易日以上的实时影子运行。
- 波浪模块按当前决定暂不实现。
